export interface BlogType {
  id?: string;
  title: string;
  body: string;
  author: string;
}

export interface RegisterType {
  id?: string;
  email: string;
  password: string;
  firstName: string;
  lastName: string;
}
